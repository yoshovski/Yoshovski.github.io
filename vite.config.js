import { defineConfig } from 'vite';

export default defineConfig({
  root: '.', // Imposta la directory principale come root
  build: {
    outDir: 'dist', // Directory di output per i file di build
  },
});